name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    uses: VibeSmiths/.github/.github/workflows/docker-build.yml@main
    with:
      image-name: mcp-rvc
